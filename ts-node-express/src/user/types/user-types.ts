export type UserInfo = {
  email: string;
  password: string;
  confirmpassword: string;
};

export type UserLogin = {
  email: string;
  password: string;
};

export type UserLogout = {
  email: string;
};
